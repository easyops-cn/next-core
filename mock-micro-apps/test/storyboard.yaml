app:
  name: Test
  id: test
  homepage: /test
  noAuthGuard: true
routes:
- path: ${APP.homepage}
  context:
  - name: a
    value: 'a:b'
  - name: x
    resolve:
      useProvider: basic.have-more-fun
      args:
      - '<% CTX.r %>'
      - 1000
      transform:
        value: '<% `{{${DATA}}}` %>'
  - name: x
    if: '<% !CTX.r %>'
    resolve:
      useProvider: basic.have-more-fun
      args:
      - '<% CTX.r %>'
      transform:
        value: '<% `{${DATA}}` %>'
  - name: y
    resolve:
      useProvider: basic.have-more-fun
      args:
      - '<% CTX.r %>'
      - 1000
      transform:
        value: '<% `「${DATA}」` %>'
    onChange:
      action: console.log
      args:
      - context changed
      - y
      - '<% EVENT.detail %>'
  - name: z
    value: '<% `z:${CTX.y}` %>'
    track: true
    onChange:
      action: console.log
      args:
      - context changed
      - z
      - '<% EVENT.detail %>'
  - name: r
    resolve:
      useProvider: basic.have-fun
  bricks:
    - brick: basic.y-button
      properties:
        label: "Hello:"
        textContent: world
      events:
        oops:
        # - action: context.replace
        #   args:
        #   - y
        #   - '<% `clicked:${CTX.y}` %>'
        - action: context.refresh
          args:
          - y
          callback:
            success:
              action: console.log
              args:
              - context replaced
              - '<% EVENT.detail %>'
              - '<% CTX.y %>'
              - '<% CTX.z %>'
        - action: console.log
          args:
          - '<% EVENT.detail %>'
          - '<% CTX.x %>'
          async: true
        - action: console.log
          args:
          - '<% EVENT.detail %>'
          - immediately
    # - brick: basic.y-button
    #   properties:
    #     label: "你好:"
    #     textContent: 世界
    - brick: basic.button-group
    - brick: div
      properties:
        title: '<% CTX.y %>'
      slots:
        "":
          type: bricks
          bricks:
          - brick: form.f-input
            properties:
              label: "Name:"
          - brick: form.f-select
            properties:
              label: |
                <% `!Gender (${PROCESSORS.basic.sayHello()}) <${CTX.x}>` %>
    # - brick: basic.have-fun
    # - brick: basic.have-more-fun
    # - brick: basic.have-no-fun
