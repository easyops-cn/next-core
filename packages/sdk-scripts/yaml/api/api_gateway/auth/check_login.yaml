_version_: 2.0
_kind_: interface

version: 1.0.0
name: CheckLogin
description: 判断用户是否登录

import: []

endpoint:
  method: GET
  uri: /api/auth/login

request: ~

response:
  type: object
  fields:
    - name: loggedIn
      type: bool
      description: 是否已经登录
